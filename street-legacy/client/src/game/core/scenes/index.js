/**
 * Core Scenes
 *
 * Generic scene base classes for Phaser games.
 * Game-specific scenes should extend these base classes.
 */

export { CoreBaseScene } from './CoreBaseScene'
// CoreBootScene, CorePreloadScene will be added as they're migrated
